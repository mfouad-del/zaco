services:
  - type: web
    name: archivx-backend
    env: node
    plan: free
    buildCommand: cd backend && npm install && npx prisma generate && npm run build
    startCommand: cd backend && npm run start:prod
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: archivx-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true

databases:
  - name: archivx-db
    plan: free
    databaseName: archivx
    user: archivx
